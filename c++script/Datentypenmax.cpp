//Datentypenmax.cpp
#include <iostream>
#include <climits>

using namespace std;

int main ()

{
cout << "Die Minimale groesse für CHAR_MIN betraegt: " << CHAR_MIN << endl;
cout  << "Die Maximale groesse für CHAR_MAX betraegt: "  << CHAR_MAX << endl;
cout << "Die Minimale groesse für WCHAR_MIN betraegt: " << WCHAR_MIN << endl;
cout << " Maximale groesse für WCHAR_MAX betraegt: " << WCHAR_MAX << endl;
return 0;
}